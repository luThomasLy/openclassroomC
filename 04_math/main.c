#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    //fabs
//    double absolu = 0, nombre = -27;
//    absolu = fabs(nombre); // absolu vaudra 27
//    printf("%lf", absolu);

    //ceil
//    double dessus = 0, nombre = 52.71;
//    dessus = ceil(nombre); // dessus vaudra 53
//    printf("%lf", dessus);

    //pow
//    double resultat = 0, nombre = 0, puissance = 0;
//    pow(nombre, puissance);
//    nombre = 2;
//    puissance = 3;
//    resultat = pow(nombre, puissance);
//    printf("%lf", resultat);

    //sqrt
    double resultat = 0, nombre = 100;
    resultat = sqrt(nombre); // resultat vaudra 10
    printf("%lf", resultat);


    return 0;
}
